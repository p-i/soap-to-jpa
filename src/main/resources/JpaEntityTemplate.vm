package $package;

import javax.persistence.Entity;
import javax.persistence.Table;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;

/**
 * GENERATED JPA CONTENT. DO NOT MODIFY!
 *
 * generated by soap-to-jpa Maven plugin
 */
@Entity
@Table(name = "${className}")
public class ${className}JPA {

    @Id
    @GeneratedValue
    private Long id;

## Generate the list of private fields
#foreach( $field in $fieldMap.keySet() )
    private $fieldMap.get($field) ${field};
#end

    /**
     * Constructor, that restores an instance of ${className}JPA from SOAP stub.
     *
     * @param stub - object of type <code>${interfaceType}</code>
     */
    public ${className}JPA(${interfaceType} stub) {
    #foreach( $field in $fieldMap.keySet() )
    this.${field} = stub.get${display.capitalize($field)}();
    #end
}

    /**
     * Get the entity ID
     */
    public Long getId() {
        return this.id;
    }

    /**
     * Id to be set
     */
    public void setId(Long id) {
        this.id = id;
    }

    ## Generate getters and setters
    #foreach( $field in $fieldMap.keySet() )

    /**
     * Get the entity ${field}
     */
    public ${fieldMap.get($field)} get${display.capitalize($field)}() {
        return this.${field};
    }

    /**
     * ${field} to be set
     */
    public void set${display.capitalize($field)}(${fieldMap.get($field)} val) {
        this.${field} = val;
    }

    #end

}